export type Config = {
    clientId: string,
    clientSecret: string,
    projectId: string,
    refreshToken: string,
    vencoder: string
    vdecoder: string
    aencoder: string
    adecoder: string
}
