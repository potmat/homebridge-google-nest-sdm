export type Config = {
    clientId: string,
    clientSecret: string,
    projectId: string,
    refreshToken: string,
    vEncoder: string,
    vDecoder: string,
    maxFps: number,
    aEncoder: string,
    aDecoder: string
}
