export type Config = {
    clientId: string,
    clientSecret: string,
    projectId: string,
    refreshToken: string,
    vEncoder: string,
    vDecoder: string,
    aEncoder: string,
    aDecoder: string,
    maxFps: number,
}
